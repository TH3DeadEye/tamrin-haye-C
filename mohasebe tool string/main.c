#include <stdio.h>
#include <string.h>
void main(){
    int chars,i;
    char reshte[chars];
    scanf("%d", &chars);
    scanf("%s", &reshte);
    
        int len1 = strlen(reshte);
        printf("%d\n", len1);
    
   
}